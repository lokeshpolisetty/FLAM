# Python bytecode and caches
__pycache__/
*.py[cod]

# Virtual environments
.venv/
venv/

# SQLite database files (runtime artifacts)
*.db
*.db-shm
*.db-wal

# Test artifacts
.pytest_cache/
.coverage

# macOS metadata
.DS_Store

# Repository initialisation script — used only to seed the local git history.
# Not part of the published project source.
init_git_history.sh
